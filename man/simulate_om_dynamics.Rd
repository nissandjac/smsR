% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/om_dynamics_engine.R
\name{simulate_om_dynamics}
\alias{simulate_om_dynamics}
\title{Shared population-dynamics engine for smsR operating models}
\usage{
simulate_om_dynamics(
  df,
  nyear,
  year,
  year_1,
  nage,
  age,
  maxage,
  nseason,
  nspace,
  M,
  weca,
  west,
  mat,
  selin,
  movemat,
  Ninit,
  R0,
  SSB_0,
  Fseason_fun,
  Rin_fun = NULL,
  env = NULL,
  stochastic = TRUE
)
}
\arguments{
\item{df}{List of OM parameters (recruitment settings, proportions, survey
config, etc. - the season loop reads these directly from \code{df}).}

\item{nyear}{Integer. Number of years to simulate.}

\item{year, year_1}{Year labels (length \code{nyear} and \code{nyear + 1}) used as dimnames.}

\item{nage, age, maxage}{Age dimension, age labels, and maximum age.}

\item{nseason, nspace}{Number of seasons and spatial areas.}

\item{M, weca, west, mat, selin}{Time-indexed arrays, dim \code{c(nage, >=nyear, 1, nseason)}.}

\item{movemat}{Movement array, dim \code{c(nage, nyear, 1, nseason)}.}

\item{Ninit}{Numbers-at-age used to initialize year 1.}

\item{R0}{Unfished recruitment, used by the stock-recruit formulas.}

\item{SSB_0}{Unfished SSB (NA if not used by the chosen recruitment model).}

\item{Fseason_fun}{function(yr, season, space, sel_season) returning the
realized F-at-age vector for that season/space.}

\item{Rin_fun}{function(yr, space) returning recruitment when
\code{df$recruitment == "estimated"} (unused for other recruitment models).
The historical run indexes \code{df$Rin} directly by \code{yr}; a forecast
cannot do that (there is no future \code{df$Rin}) and must supply its own
closure, e.g. a geometric mean of recent historical recruitment.}

\item{env}{Optional environmental covariate matrix, indexed as \code{env[yr, ]}
(already aligned to the engine's internal \code{yr} index by the caller).}

\item{stochastic}{Logical. If \code{FALSE}, recruitment deviations are drawn as zero.}
}
\value{
A list of the raw (noise-free) simulation outputs shared by both callers.
}
\description{
Internal helper that runs the season/year loop (recruitment, mortality,
movement, catch and survey calculation) shared by \code{\link{run.agebased.sms.op}}
(historical OM run) and \code{\link{forecast_op}} (OM forecast). Callers are
responsible for building the time-indexed input arrays and for supplying an
\code{Fseason_fun} closure that encodes how fishing mortality is derived in
their context (extracted from \code{df} for the historical run, user-supplied
for the forecast).
}
\keyword{internal}
